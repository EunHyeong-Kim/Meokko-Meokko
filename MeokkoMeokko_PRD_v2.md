# [머꼬머꼬] 제품 요구사항 정의서 (PRD) v2

> v2 업데이트 일자: 2026-02-20
> v1 대비 변경사항: 검색 엔진 전환(네이버→카카오 좌표 기반), 카테고리 UI 개편, 키워드 대폭 확장, 지도 제거, 페이지네이션 추가, 뒤로가기 네비게이션 추가

---

## 1. 프로젝트 개요

- **프로젝트명**: 머꼬머꼬 (Meokko Meokko)
- **목표**: 사용자의 현재 위치 또는 검색된 주소 **반경 1km** 내의 맛집을 6가지 감정/맛 카테고리로 큐레이션하여 추천하는 웹 서비스
- **플랫폼**: 웹 애플리케이션 (Web) - 데스크탑 및 모바일 반응형 지원
- **핵심 타겟**: 점심 메뉴 결정 장애를 겪는 직장인, 데이트 코스를 찾는 연인, 다이어터

---

## 2. 기술 스택 (Tech Stack)

| 구분 | 기술 |
|------|------|
| Framework | Next.js 14 (App Router) |
| Styling | Tailwind CSS |
| Icons | Lucide React |
| Backend/DB | Supabase (Auth, Postgres Database) |
| 맛집 검색 API | **Kakao Local Search API** (좌표 기반 반경 검색) |
| 위치 검색 API | Naver Open API (지역 검색 - 지오코딩용) |
| 인증 | Supabase Auth (카카오 로그인, 이메일 로그인) |

### API 키 구성 (.env.local)

| 키 | 용도 | 출처 |
|----|------|------|
| `NEXT_PUBLIC_KAKAO_JS_KEY` | 카카오 지도 SDK (현재 미사용) | 카카오 앱 |
| `KAKAO_REST_API_KEY` | 맛집 좌표 기반 검색 | 카카오 일반 앱 (지도/로컬 서비스 활성 필수) |
| `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` | 위치 검색 (지오코딩) | 네이버 Open API |
| `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase 연동 | Supabase 프로젝트 |

---

## 3. 사용자 흐름 (User Flow)

```
로그인 → 위치 설정 → 카테고리 선택 → 맛집 결과 리스트 → 상세 정보 / 즐겨찾기
```

1. **로그인 (Login)**: 카카오 로그인 또는 이메일/비밀번호 로그인
2. **위치 설정 (Location)**: "어디서 드실 건가요?" → [현재 위치 찾기] 또는 [장소 검색]
3. **카테고리 선택 (Category)**: "오늘 뭐 땡겨?" → 6개 테마 중 택 1 (이모지 + 타이틀만 표시, 음식명 미표시)
4. **결과 리스트 (Results)**: 반경 1km 내 맛집 리스트 (10개씩 페이지네이션, 최대 5페이지)
5. **상세 정보**: 카드 내에서 주소, 전화번호, 상세 정보 링크 바로 확인
6. **즐겨찾기**: 하트 아이콘으로 저장/해제, 즐겨찾기 페이지에서 모아보기

---

## 4. 핵심 검색 아키텍처

### 검색 흐름

```
사용자 위치(좌표) + 카테고리 선택
    ↓
카테고리 키워드 10~15개 중 랜덤 3개 추출
    ↓
각 키워드에 "맛집" 붙여서 카카오 API 호출 (3회)
  예: "마라탕 맛집" + x=127.03 + y=37.50 + radius=1000
    ↓
최대 45개 결과 → 중복 제거 → 거리순 정렬
    ↓
10개씩 페이지네이션하여 표시
```

### 카카오 API 호출 형식

```
GET https://dapi.kakao.com/v2/local/search/keyword.json
  ?query={키워드} 맛집
  &x={경도}
  &y={위도}
  &radius=1000
  &size=15
  &sort=accuracy
Headers: Authorization: KakaoAK {REST_API_KEY}
```

### 주의사항
- 카카오 **비즈 앱**은 지도/로컬 API 사용 시 유료 추가 기능 신청 필요
- **일반 앱**의 REST API 키를 사용해야 무료로 좌표 기반 검색 가능
- 일일 무료 쿼터: 100,000건

---

## 5. 카테고리 정의 (6 Themes)

카테고리 선택 화면에서는 **이모지 + 타이틀만** 표시 (음식명 미표시).

### 🍚 한국인은 밥심 (bapsim) — 15개 키워드
한식, 백반, 솥밥, 한정식, 제육, 김치찌개, 된장찌개, 비빔밥, 불고기, 갈비, 쌈밥, 순두부, 보쌈, 정식, 돌솥밥

### 🍔 치팅 데이 (cheating) — 13개 키워드
햄버거, 피자, 파스타, 중식, 치킨, 탕수육, 짜장면, 짬뽕, 돈가츠, 스테이크, 족발, 곱창, 돈까스

### 🔥 스트레스 해소 (stress) — 12개 키워드
마라탕, 떡볶이, 불닭, 매운갈비찜, 닭발, 마라샹궈, 엽떡, 낙곱새, 매운라면, 쭈꾸미, 불막창, 핫치킨

### ♨️ 뜨끈뜨끈 (hot) — 14개 키워드
국밥, 찌개, 칼국수, 우동, 전골, 설렁탕, 갈비탕, 삼계탕, 수제비, 감자탕, 부대찌개, 샤브샤브, 라멘, 뼈해장국

### 🥗 가볍고 간단하게 (fresh) — 11개 키워드
포케, 샐러드, 샌드위치, 그릭요거트, 베이글, 키토김밥, 닭가슴살, 월남쌈, 오트밀, 토스트, 아사이볼

### 🍰 혈당 끌어 올려 (sugar) — 13개 키워드
카페, 베이커리, 케이크, 디저트, 탕후루, 아이스크림, 크로플, 마카롱, 도넛, 붕어빵, 호떡, 와플, 초콜릿

---

## 6. 화면별 상세 사양

### A. 로그인 페이지 (`/login`)
- 중앙 정렬 로고 + "카카오로 시작하기" 버튼
- 이메일/비밀번호 로그인 폼
- 회원가입 전환 링크
- 배경: 주황-아이보리 그라데이션

### B. 위치 설정 페이지 (`/location`)
- **현재 위치 찾기**: 브라우저 Geolocation API 활용
- **장소 검색**: 네이버 지역 검색 API로 장소명 → 좌표 변환
- 검색 결과 리스트에서 장소 선택 시 좌표와 장소명 전달
- 뒤로가기 버튼 포함

### C. 카테고리 선택 페이지 (`/category`)
- "오늘 뭐 땡겨?" 타이틀
- 2열 × 3행 그리드 (sm 이상 3열)
- 각 카드: **이모지 + 타이틀만** 표시 (음식명 미표시)
- 카드별 고유 배경색 + 호버 시 테두리 강조

### D. 결과 페이지 (`/results`)
- **지도 없음** (제거됨)
- 타이틀: 카테고리 이모지 + 라벨 (가운데 정렬, 큰 글씨)
- 부제: "반경 1km 내 {N}개의 맛집"
- 맛집 카드 (최대폭 `max-w-md`, 가운데 정렬):
  - 매장명 + 거리(m) + 즐겨찾기 하트
  - 주소
  - 전화번호 (터치 시 바로 전화 연결)
  - "상세 정보" 링크 (카카오맵 상세 페이지로 이동)
- **페이지네이션**: 10개씩, 하단에 1~5 페이지 버튼 (현재 페이지 주황 강조)
- 페이지 전환 시 자동 맨 위 스크롤

### E. 즐겨찾기 페이지 (`/favorites`)
- 저장된 맛집 리스트
- 각 카드: 매장명, 주소, 카테고리 설명, 저장 날짜, 상세 정보 링크
- 삭제(휴지통) 버튼
- 뒤로가기 버튼 포함

---

## 7. 공통 UI 컴포넌트

### Header
- 고정 상단 (`sticky top-0`)
- 로고 (카테고리 페이지로 이동)
- 즐겨찾기 아이콘 + 로그아웃 아이콘
- `showBack` prop: 하위 페이지에서 뒤로가기 화살표 표시 (위치설정, 결과, 즐겨찾기)

---

## 8. 데이터베이스 스키마 (Supabase)

### profiles
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | auth.users 참조 |
| nickname | text | 사용자 닉네임 |
| avatar_url | text | 프로필 사진 |
| created_at | timestamptz | 생성일 |

### restaurants
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | bigint (PK) | 자동 증가 |
| name | text | 가게 이름 |
| category | text | bapsim/cheating/stress/hot/fresh/sugar |
| lat | float8 | 위도 |
| lng | float8 | 경도 |
| address | text | 주소 |
| image_url | text | 카카오맵 place_url 저장용 |
| description | text | 카테고리 설명 |
| created_at | timestamptz | 생성일 |

### favorites
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | bigint (PK) | 자동 증가 |
| user_id | uuid (FK) | profiles.id 참조 |
| restaurant_id | bigint (FK) | restaurants.id 참조 |
| created_at | timestamptz | 생성일 |
| | | UNIQUE(user_id, restaurant_id) |

### reviews
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | bigint (PK) | 자동 증가 |
| restaurant_id | bigint (FK) | restaurants.id 참조 |
| user_id | uuid (FK) | profiles.id 참조 |
| rating | int | 1~5 |
| comment | text | 리뷰 내용 |
| created_at | timestamptz | 생성일 |

### RLS 정책

| 테이블 | 작업 | 정책 |
|--------|------|------|
| profiles | SELECT | 누구나 조회 가능 |
| profiles | UPDATE | 본인만 수정 가능 |
| restaurants | SELECT | 누구나 조회 가능 |
| restaurants | INSERT | 로그인한 유저 등록 가능 |
| favorites | SELECT | 본인 즐겨찾기만 조회 |
| favorites | INSERT | 로그인한 유저만 추가 |
| favorites | DELETE | 본인만 삭제 |
| reviews | SELECT | 누구나 조회 가능 |
| reviews | INSERT | 로그인한 유저만 작성 |
| reviews | DELETE | 본인만 삭제 |

---

## 9. 디자인 가이드

| 항목 | 값 |
|------|-----|
| Primary Color | Orange (#FF6B00) |
| Category - 밥심 | #FF6B00 (bg: #FFF3E8) |
| Category - 치팅 | #FFD600 (bg: #FFFDE7) |
| Category - 스트레스 | #EF4444 (bg: #FEF2F2) |
| Category - 뜨끈뜨끈 | #F97316 (bg: #FFF7ED) |
| Category - 가볍게 | #4ADE80 (bg: #F0FDF4) |
| Category - 혈당 | #F472B6 (bg: #FDF2F8) |
| Font | Pretendard |
| 버튼 최소 높이 | 48px (모바일 터치 고려) |
| 카드 최대 너비 | max-w-md (가운데 정렬) |

---

## 10. v1 대비 주요 변경 이력

| 항목 | v1 | v2 |
|------|-----|-----|
| 검색 엔진 | 네이버 텍스트 기반 검색 | **카카오 좌표 기반 반경 검색** |
| 카테고리 타이틀 | "오늘은 밥심" | **"한국인은 밥심"** |
| 카테고리 카드 | 음식명 표시 | **이모지 + 타이틀만** |
| 치팅 데이 이모지 | 🍕 | **🍔** |
| 뜨끈뜨끈 이모지 | 🥘 | **♨️** |
| "가볍게 간단하게" | "가볍게 간단하게" | **"가볍고 간단하게"** |
| 키워드 수 | 카테고리당 3~5개 | **카테고리당 11~15개** |
| 검색 방식 | 전체 키워드 순차 검색 | **랜덤 3개 추출 후 검색** |
| 결과 표시 | 15개 고정 | **전체 결과 (최대 45개), 10개씩 페이지네이션** |
| 지도 | 카카오맵 표시 | **제거** |
| 네비게이션 | 뒤로가기 없음 | **Header에 뒤로가기 화살표 추가** |
| 미들웨어 | API 경로 포함 인증 체크 | 동일 (추후 API 경로 제외 검토) |
